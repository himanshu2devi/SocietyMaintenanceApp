# Production API hostnames (unchanged DNS → EC2). Frontend branding is on Vercel.
identity.societywale.in {
	encode gzip
	reverse_proxy identity:8081
}

core.societywale.in {
	encode gzip
	reverse_proxy core:8082
}
